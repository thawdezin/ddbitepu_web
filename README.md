# DD BitePu Website

The public DD BitePu website and Android APK distribution frontend. It includes the web entry point and Vercel deployment configuration.

Copy required local settings from `.env.example`; do not commit secrets from `.env.local`.
